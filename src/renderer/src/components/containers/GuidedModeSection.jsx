import { Stack } from "@mantine/core";

const GuidedModeSection = ({ children }) => {
  return (
    <Stack gap="sm" w="100%">
      {children}
    </Stack>
  );
};

export default GuidedModeSection;
