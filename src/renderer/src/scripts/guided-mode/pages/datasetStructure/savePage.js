import { guidedSkipPage, guidedUnSkipPage } from "../navigationUtils/pageSkipping";
import useGlobalStore from "../../../../stores/globalStore";

export const savePageDatasetStructure = async (pageBeingLeftID) => {
  const errorArray = [];

  if (pageBeingLeftID === "1") {
    console.log("hi");
  }
};
